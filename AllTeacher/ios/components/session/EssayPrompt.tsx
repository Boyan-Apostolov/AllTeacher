/**
 * RETIRED — kept as an empty stub because the file system this project
 * runs on doesn't allow deletion via tooling. The essay_prompt exercise
 * type was removed in favour of `short_answer` with a rubric. Nothing
 * imports this file; legacy `essay_prompt` rows in the DB are routed to
 * ShortAnswer by `ExerciseView`.
 */
export {};
